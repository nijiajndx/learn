apply plugin: 'java'
apply plugin:'groovy'

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile(
            'org.codehaus.groovy:groovy-all:2.4.0-beta-3'
    )
    testCompile group: 'junit', name: 'junit', version: '4.11'
}